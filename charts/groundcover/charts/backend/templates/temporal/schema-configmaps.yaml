{{- if .Values.global.temporal.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: temporal-schema-files
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: temporal
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: schema
    {{- include "groundcover.labels" . | nindent 4 }}
data:
  {{- $files := .Files }}
  {{- range $path, $_ := .Files.Glob "temporal/sql-schemas/temporal/*.sql" }}
  {{- $fileName := base $path }}
  {{ $fileName | replace "." "_" }}: |
    {{- $files.Get $path | nindent 4 }}
  {{- end }}
  {{- range $path, $_ := .Files.Glob "temporal/sql-schemas/temporal/versioned/**/*.sql" }}
  {{- $fileName := base $path }}
  {{- $dirName := base (dir $path) }}
  {{ printf "%s_%s" $dirName $fileName | replace "." "_" }}: |
    {{- $files.Get $path | nindent 4 }}
  {{- end }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: temporal-visibility-schema-files
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: temporal
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: schema
    {{- include "groundcover.labels" . | nindent 4 }}
data:
  {{- $files := .Files }}
  {{- range $path, $_ := .Files.Glob "temporal/sql-schemas/visibility/*.sql" }}
  {{- $fileName := base $path }}
  {{ $fileName | replace "." "_" }}: |
    {{- $files.Get $path | nindent 4 }}
  {{- end }}
  {{- range $path, $_ := .Files.Glob "temporal/sql-schemas/visibility/versioned/**/*.sql" }}
  {{- $fileName := base $path }}
  {{- $dirName := base (dir $path) }}
  {{ printf "%s_%s" $dirName $fileName | replace "." "_" }}: |
    {{- $files.Get $path | nindent 4 }}
  {{- end }}
{{- end }}