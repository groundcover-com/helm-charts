{{- if and .Values.backend.enabled .Values.shepherd.httpIngress.enabled }}
{{- $ingressApiIsStable := eq (include "shepherd.ingress.isStable" .) "true" -}}
{{- $ingressSupportsIngressClassName := eq (include "shepherd.ingress.supportsIngressClassName" .) "true" -}}
{{- $ingressSupportsPathType := eq (include "shepherd.ingress.supportsPathType" .) "true" -}}
{{- $servicePort := .Values.shepherd.service.httpPort -}}
{{- $ingressPathType := .Values.shepherd.httpIngress.pathType -}}
apiVersion: {{ include "shepherd.ingress.apiVersion" . }}
kind: Ingress
metadata:
{{- if .Values.shepherd.httpIngress.annotations }}
  annotations:
{{ toYaml .Values.shepherd.httpIngress.annotations | indent 4 }}
{{- end }}
  labels:
    {{ with .Values.global.groundcoverLabels }} 
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{ with .Values.shepherd.httpIngress.extraLabels }}
{{ toYaml . | indent 4 }}
  {{ end }}
  name: {{ include "shepherd.name" . }}-http
  namespace: {{ .Release.Namespace }}
spec:
  {{- if and $ingressSupportsIngressClassName .Values.shepherd.httpIngress.ingressClassName }}
  ingressClassName: {{ .Values.shepherd.httpIngress.ingressClassName }}
  {{- end }}
  rules:
  {{- $serviceName := (include "shepherd.name" .) }}
  {{- range .Values.shepherd.httpIngress.hosts }}
  - host: {{ .host }}
    http:
      paths:
        - path: {{ .path }}
          {{- if $ingressSupportsPathType }}
          pathType: {{ $ingressPathType }}
          {{- end }}
          backend:
            {{- if $ingressApiIsStable }}
            service:
              name: {{ $serviceName }}
              port:
                number: {{ $servicePort }}
            {{- else }}
            serviceName: {{ $serviceName }}
            servicePort: {{ $servicePort }}
            {{- end }}
  {{- end -}}
{{- if .Values.shepherd.httpIngress.tls }}
  tls:
{{ toYaml .Values.shepherd.httpIngress.tls | indent 4 }}
{{- end -}}
{{- end -}}
